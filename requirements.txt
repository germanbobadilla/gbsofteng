# Core framework
Django>=5.2,<5.3
wagtail>=7.1,<7.2

# Database
psycopg2-binary>=2.9,<3.0   # PostgreSQL driver

# Environment variables
django-environ>=0.11,<0.12  # For managing .env settings

# Static & media files
whitenoise>=6.6,<6.7        # Serve static files (esp. for dev/Docker)
django-storages>=1.14,<1.15 # Storage backends (S3, etc.)
boto3>=1.34,<1.35           # AWS SDK for S3

# Images
Pillow>=10.0,<11.0          # Required for Wagtail images

# Gunicorn (production server)
gunicorn>=22.0,<23.0

